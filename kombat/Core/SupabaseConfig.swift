//
//  SupabaseConfig.swift
//  kombat
//

import Foundation

enum SupabaseConfig {
    /// TODO: replace with your project URL, e.g. "https://xxxxxxxxxxxx.supabase.co"
    static let projectURL = URL(string: "https://REPLACE_WITH_YOUR_PROJECT_REF.supabase.co")!

    /// Safe to ship in client code — this is the publishable (anon) key, not the secret key.
    static let publishableKey = "sb_publishable_NeEotl6mc7NpNN71fTAg0g_rJroT1-4"
}
